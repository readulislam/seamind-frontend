import { Container } from '@mui/material';
import React from 'react';
import GoalCardView from './GoalCardView';

const GoalCard = ({ props }) => {
  return <GoalCardView props={props} />;
};

export default GoalCard;
